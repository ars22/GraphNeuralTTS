
audio:
    num_mels: 80
    num_freq: 1025
    sample_rate: 16000 
    frame_length_ms: 50
    frame_shift_ms: 12.5
    preemphasis: 0.97
    min_level_db: -100
    ref_level_db: 20
    power: 1.5
    griffin_lim_iters: 60

model:
    optimizer:
        type: "Adam"
        lr: 0.002
        weight_decay: 0.0
        betas: [0.9, 0.999]

    tacotron:
        n_vocab: 250 # Use a larger vocab size is ok.
        embedding_size: 256
        add_info_embedding_size: 32
        mel_size: 80
        linear_size: 1025
        r: 5

solver:
    # Data config. 
    meta_path: {'train': 'training-accentdb-char-baseline-with-additional-info/meta_train_hrg.txt', 'test': 'training-accentdb-char-baseline-with-additional-info/meta_test_hrg.txt'}
    data_dir: 'training-accentdb-char-baseline-with-additional-info'
    n_jobs: 4
    # Training config.
    batch_size: 32
    grad_clip: 1.0
    total_steps: 400000
    validation_interval: 2000
    save_checkpoint_interval: 10000
    # log
    log_interval: 10
    add_info_headers: ["accent", "speaker"]


