
audio:
    num_mels: 80
    num_freq: 1025
    sample_rate: 16000 
    frame_length_ms: 50
    frame_shift_ms: 12.5
    preemphasis: 0.97
    min_level_db: -100
    ref_level_db: 20
    power: 1.5
    griffin_lim_iters: 60

model:
    optimizer:
        type: "Adam"
        lr: 0.002
        weight_decay: 0.0
        betas: [0.9, 0.999]

    tacotron:
        n_vocab: 250 # Use a larger vocab size is ok.
        embedding_size: 128
        add_info_embedding_size: 32
        gcn_hidden_size: 128
        mel_size: 80
        linear_size: 1025
        r: 5

solver:
    # Data config. 
    meta_path: {'train': 'training-arcticdb/meta_train.txt', 'test': 'training-arcticdb/meta_val.txt'}
    data_dir: 'training-arcticdb'
    n_jobs: 0
    # Training config.
    batch_size: 32
    grad_clip: 1.0
    total_steps: 400000
    validation_interval: 2000
    save_checkpoint_interval: 10000
    # log
    log_interval: 10
    add_info_headers: ["speaker", "accent", "gender"]